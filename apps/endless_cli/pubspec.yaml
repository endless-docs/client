name: endless_cli
description: Endless Docs command-line Local API client.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.10.0

resolution: workspace

dependencies:
  args: ^2.7.0
  local_api: any
  local_api_client: any
  platform_runtime: any

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
