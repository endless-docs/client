name: endless_app
description: Offline-first Endless Docs desktop client.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.10.0

resolution: workspace

dependencies:
  codex_app_server: any
  flutter:
    sdk: flutter
  flutter_markdown_plus: ^1.0.12
  local_api: any
  local_api_client: any
  platform_runtime: any

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
