name: platform_runtime
description: Endless Docs profile paths, process locks, and daemon discovery.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.10.0

resolution: workspace

dependencies:
  local_api: any
  local_api_client: any

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
