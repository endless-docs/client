name: local_api
description: Transport-neutral Endless Docs Local API messages.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.10.0

resolution: workspace

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
