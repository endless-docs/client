name: local_backup
description: Versioned streaming backup archive codec for Endless Docs profiles.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.10.0

resolution: workspace

dependencies:
  client_application: any
  client_domain: any
  crypto: ^3.0.7

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
